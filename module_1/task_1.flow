import lib/runtime;
import lib/string;

main() {
	println("Task 1");
	a : [int] = [1, 2, 3];
	map(a, \i -> println(i));
	strArray: [string] = map(a, i2s);
	println(strGlue(strArray, " "));
	println("cast from int to string was finally");
}
